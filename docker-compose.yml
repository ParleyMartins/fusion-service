version: '3.6'

services:
    db:
        image: postgres:alpine
        ports:
            - '5432:5432'
#         volumes:
#             - postgres_data:/var/lib/postgresql/data/

# volumes: postgres_data:
